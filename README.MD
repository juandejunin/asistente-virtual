Crear el servicio del clima (WeatherService.ts) que consulte OpenWeatherMap.

Crear el controlador y la ruta /api/weather/today para probar manualmente la respuesta del clima en tu navegador o Postman.

Luego conectaremos el servicio de Telegram y finalmente el worker que lo ejecute cada mañana.